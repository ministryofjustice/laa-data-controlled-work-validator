group=uk.gov.justice.laa

# Disable JVM Class Data Sharing to suppress warning:
# "Sharing is only supported for boot loader classes because bootstrap classpath has been appended"
# This warning occurs because Gradle/Spring Boot modifies the classpath at runtime.
org.gradle.jvmargs=-Xshare:off


